# Дата: 2026-03-29

 ### Что было сделано:  

  - Полоностью закончила `Header`. Разобралась со стилями и CSS-переменными. После добавления к странице стал некорректно отображаться, прямо в центре страницы, хотя до этого, когда добавлялся к `body` всё работало отлично. Решением стало повесить на родительский элемент такие стили:
  ```css
  #app {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  width: 100%;
  min-height: 100vh;
}
  ```
  - Сделала рефакторинг выпадающего меню, теперь пункты в нём меняются динамически, и ссылка на текущую страницу не отображается.
  - Добавила отображение текущего имени пользователя, которое беру через общую функцию `getCurrentUserName`.
  - Создала переиспользуемый компонент, для более интересного отображения для ошибок. Задала параметры со значениями по умолчанию: `Title`, `Message`, `onRetry()`. Все они опциональные. `onRetry` - отвечает за то, будет ли отображаться кнопка с предложением, например, повторить запрос.
  - Добавила звуки в `Async Sorter`. Попросила AI создать четыре вида фоновых звуков: для успешного ответа, для ошибки, для клика и для отмены действия. Надеюсь, успею заменить на нормальные. У меня осталась целая коллекция, когда выбирала звуки для RSS-Puzzle, который из-за праздников так и не закончила.
  - Добавила в `Async Sorter` проверку на то есть, ли уже выбранный слот в ответе, чтобы не происходило дублирования. 

### Проблемы:  
Решала конфликты в двух ветках, что заняло немало времени. В первой всё прошло успешно, а во вотрой случайно нажала на принятие не того изменения - и час потерян. Конфликты - это совсем не страшно, только если они не касаются настроек и зависимостей😊. Вспомнила, это было с `Dashboard`, случайно убрала свои изменения, касающиеся настроек `D3`. И тут началось. Вроде конфликт разрешён, но ничего не рисует. npm install не помог, но вдруг потом выплыл `package-lock.json` с Merge Editor новом обличье. Где можно было переносить изменения из левой части в правую, а не трёхоконный. Наверное был ещё один npm install - и диаграммы закрутились!  
Не получается вплотную заняться сервером. Не понимаю, нужно ли настраивать переменные для **Netlify** или можно будет запускать локально. В голове куча организационных вопросов, как это совместить и ничего не поломать, а действовать страшно, так как отвечаю не только за себя. Вот и сижу гуглю, расписываю варианты. Почему не сохраняются новые изменения в базу в  Async Sorter, пока не разобралась. Радует, что хоть со всем остальным закончила, осталось только отобразить настоящие данные на `Dashboard`.


### Мысли:  
Продержаться!

